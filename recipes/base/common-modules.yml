modules:
  - type: dnf
    repos:
      copr:
        enable:
          - scottames/ghostty
          - atim/starship
          - dejan/lazygit
    install:
      install-weak-deps: false
      packages:
        # terminal / shell
        - ghostty
        - starship

        # cli
        - fzf
        - git
        - lazygit
        - neovim
        - tree-sitter-cli
        # - eza Change the mean of install
        - btop
        - fastfetch

        # utilities
        - distrobox

        # STEAM
        - steam-devices
    remove:
      packages:
        - firefox
        - firefox-langpacks

  - type: script
    scripts:
      - ensureautoupdates.sh
      - addimageinfo.sh

  - type: files
    files:
      - source: system
        destination: /

  - type: signing
